#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void printUsername();
char *input();

int numOfCommands; //global variable that keeps track of the number of commands to run for each input
